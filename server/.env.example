# Database (PostgreSQL — Railway Postgres plugin or any Postgres URL)
DATABASE_URL="postgresql://user:pass@host:5432/db"

# Server — Railway sets PORT automatically, leave unset in Railway env vars
PORT=4000

# CORS: set to your deployed admin/widget origins in production
# e.g. CORS_ORIGINS=https://your-admin.vercel.app,https://your-app.com
CORS_ORIGINS=*
